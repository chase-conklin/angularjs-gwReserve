(function (angular) {
    angular
        .module("application")

        .controller("roomReservationListComponentController", function() {
            console.log("list controller working!")
        });

}(window.angular));
