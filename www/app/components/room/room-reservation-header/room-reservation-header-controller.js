(function (angular) {
    angular
        .module("application")

        .controller("roomReservationHeaderComponentController", function() {
            console.log("header controller working!")
        });

}(window.angular));
